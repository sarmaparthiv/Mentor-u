html
  head
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css")
  body
      div.container
      h1(style="font-weight:bold") Welcome, #{user.fullname}
      table.table.table-striped.table-bordered.table-hover
        tr
          td fullname:
          td #{user.fullname}
        tr
          td username:
          td #{user.username}
        tr
          td email: 
          td #{user.email}
        tr 
          td number:
          td #{user.number}

a(href="/edit?id=#{user._id}") Edit 
br
br
button(type="button", a href='/logout') Logout
    